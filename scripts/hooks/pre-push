#!/usr/bin/env bash
set -euo pipefail

if [ ! -d build ]; then
    echo "[pre-push] no build dir, skipping tests"
    exit 0
fi

echo "[pre-push] building..."
cmake --build build -j"$(nproc)"

echo "[pre-push] running tests..."
ctest --test-dir build --output-on-failure
